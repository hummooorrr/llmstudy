spring:
  application:
    name: functioncall
  ai:
    zhipuai:
      api-key: ${ZHIPU_API_KEY:}
      chat:
        options:
          model: ${FUNCTIONCALL_CHAT_MODEL:glm-4.5-Air}

server:
  port: ${SERVER_PORT:28690}
